import React from 'react';

const NotAdapt = () => {
    return (
        <div>
            Cette page est la page avec un rendu visuel pour les personnes normale
        </div>
    );
};

export default NotAdapt;